# Local data (never commit)
store/
media/
audit.log
audit.log.*
*.db
*.db-journal
*.db-shm
*.db-wal

# Secrets and config
.env
.env.local
.env.*.local

# Go
whatsapp-bridge/whatsapp-bridge
whatsapp-bridge/whatsapp-bridge.exe
prospect-api/prospect-api.exe
whatsapp-bridge/bin/
whatsapp-bridge/vendor/
prospect-api/prospect-api
prospect-api/bin/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.venv/
.uv/
uv.lock

# Whisper models (large binaries, downloaded separately)
models/
*.bin

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Local-only operator files. These contain operator-specific runtime data,
# personal contacts, or session-specific context that should not be pushed
# to the public repo. Generated/maintained by the operator running the bridge.
bridge-stdout.log
bridge-stderr.log
prospect-api/BRIDGE-SPEC-DELTA-*.md
prospect-api/Negotiator Playbook.md
prospect-api/Negotiator Playbook *.md
prospect-api/named-tunnel-cutover.sh
# Generated MCP bundle (uploaded to GitHub Releases, not tracked in git)
*.mcpb
